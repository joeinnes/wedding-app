include includes/head.pug
main.site-content
  section.container
    .columns
      .column.is-8
        section.card.is-light
          .card-image
            img(src=locationVal.Photos[0].url)
          .card-content
            h2.title.is-size-2 #{locationVal.Name}
            p #{locationBlurb}
            div
              iframe(src=locationVal["Map URL"] width="100%" height="300" frameborder="0" style="border:0;" allowfullscreen="")
              
include includes/footer.pug